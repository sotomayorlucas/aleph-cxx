// apps/aleph_edit — the structural editor's SDL shell (Phase 6, SPEC §3.3).
//
// STUB: the hybrid raster/path-trace loop, orbit camera, pick->select, the v1
// gesture set (SetMaterial / SetTransform / AddObject / AddLight / Delete),
// idle progressive path-trace, and `--headless <script> <outdir>` land in
// Phase 6 W3. For now this is an empty-but-valid entry point so the project
// builds green (built only when ALEPH_HAVE_SDL2).
int main() {
    return 0;
}
